[phases.setup]
nixPkgs = ["...", "prisma-engines", "poppler"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate"]

[phases.start]
cmds = [
  "node src/server.js"
]
