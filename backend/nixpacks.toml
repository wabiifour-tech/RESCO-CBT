[phases.setup]
nixPkgs = ["...", "prisma-engines", "postgresql"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate"]

[phases.start]
cmds = [
  "npx prisma db push --skip-generate --accept-data-loss || echo 'prisma db push skipped'",
  "node src/server.js"
]
