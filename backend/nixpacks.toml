[phases.setup]
nixPkgs = ["...", "prisma-engines"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate"]

[phases.start]
cmds = [
  "node prisma/migrate-assignments.js || echo 'Migration script completed (may be no-op)',",
  "npx prisma db push --skip-generate --accept-data-loss 2>/dev/null || true",
  "node prisma/seed.js || echo 'Seed failed, continuing with server start'",
  "node src/server.js"
]
